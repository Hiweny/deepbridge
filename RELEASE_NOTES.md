## v1.6.2 更新内容

### 修复：压缩提示词里“没有待压缩内容”
- **根因**：旧版本压缩时会裁剪原始历史，但总轮数计数没有同步减少；升级后计数声称还有 N 轮可压、实际那些原文已不在历史里，批次索引落到历史之外 → 发给 AI 的“新增对话批次”为空（压缩空气）。
- **以历史记录为唯一事实来源**：
  - 每次加载会话立即对账：按实际历史条数修正总轮数，并把“已折叠轮数”钳制在合法范围；旧版本裁剪过的会话也能正确接续。
  - 待压缩批次完全按实际历史计算，**若某批次内容为空会直接阻止发送**，从根本上杜绝空压缩。
  - 新增对话后总轮数始终等于实际历史轮数，计数不再漂移。

### 优化：微信消息 → DeepSeek 输入/发送更快
- 重写发送等待：填入后先给框架一个极短 tick（60ms）即尝试发送、随后以 150ms 细粒度轮询发送按钮，按钮一旦可用立即点击；取代此前“固定等 300ms + 500ms 粗轮询”，常见情况每条消息少等约 0.3–0.8 秒。
- 后台记忆折叠 / 会话轮换会给新到的消息主动让路：检测到已有更新的消息进来就延后维护，避免压缩或开新对话占用发送通道而拖慢新消息。

### 保留
- 单一上下文窗口 N（可见窗口即压缩批次）、按总轮数阈值 R 轮换（默认 200，0=仅手动，可用微信 `/新对话`）、无障碍保活、实时常驻通知、图片/文件传输、全屏沉浸与深色跟随等能力不变。
- 与历史版本**同一签名密钥，可直接覆盖安装，微信连接与本地记忆保留、无需重新扫码**。
