## v1.6.1 更新内容

### 修复：压缩记忆不再每次开新对话
- **取消「每折叠一批就 New chat」**：记忆压缩只做摘要折叠与合并，不会重置 DeepSeek 网站会话。
- **网站会话轮换改为按总轮数阈值 R（默认 200，按总对话轮数计）**：只有距上次开新对话累计满 R 轮才自动 New chat，从而**最大限度利用 DeepSeek 官网聊天的上下文窗口**，不频繁开新对话。
- **R 设为 0 即关闭自动轮换、完全手动**：可用微信指令 `/新对话`（`/newchat`），或控制台「✨ 新建对话」按钮，或在对话页自己点官网 New chat。
- 控制台手动新建对话后会同步所有会话的轮换基准，避免紧接着又触发一次自动轮换。

### 保留（v1.6 能力）
- 记忆仍为「单一上下文窗口 N」的滑窗 + 分批合并：N 既是可见上下文（最近 N 轮）、也是压缩批次；原始历史完整保留、窗口不重置，所有对话都会被覆盖。
- 无障碍保活、实时常驻通知（AI 阶段 / 微信在线 / 收发条数与时间 / 重启桥接）、图片文件传输、全屏沉浸与深色跟随等保持不变。

### 说明
- 与历史版本**同一签名密钥，可直接覆盖安装，微信连接与本地记忆保留、无需重新扫码**。
