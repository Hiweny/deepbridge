## v1.5.0 更新内容

### 界面 / 沉浸
- **全屏沉浸**：内容延伸到状态栏（摄像头区域）与系统导航栏下方，像浏览器全屏一样；控制台自动避开刘海/手势条，对话页整页铺满。
- **深色模式跟随系统**：控制台与 DeepSeek 网页都会随系统在浅色/深色间自动切换（WebView 算法变暗 + 全站深色适配），切换系统主题即时生效。
- 底部标签栏、卡片与设置面板做了对比与间距优化，深色下更清晰。

### 后台保活强化（解决「后台放久了要重新打开才收消息」）
- 新增 9 分钟 AlarmManager 心跳（Doze 空闲也能周期唤醒续期）、看门狗轮询自愈、划掉最近任务 1 秒后自动重启、开机自启。
- 配合原有前台服务 + WakeLock + 电池白名单，长时间后台也能持续收消息（仍建议在系统里关闭对本应用的省电限制）。

### Prompt 工程（透明、可控、可回退）
- 默认人设改为 **DeepSeek 娘**（女性化/可爱/甜美/女仆感，称呼用户「主人」，专业问题依旧靠谱）。
- 新增「Prompt 工程」面板：**角色人设、微信规则、多条消息规则、记忆总结模板** 四段都可单独编辑、单独恢复默认或一键全部回退，并可预览最终拼好的 Prompt。
- **微信规则**：内置完整微信原生 emoji 清单与 `[烟花][炸弹][爆竹]` 全屏彩蛋用法，明确要求克制使用、单发表情最佳。
- **多条消息规则**重写：强调「想拆多条就必须输出 `\` 分隔」，给出示例，同时明确普通回复不拆、禁止滥用、最多 6 条。
- 记忆总结模板可编辑（`{budget}`/`{max}` 自动替换），自适应篇幅、默认上限 3000 字不变。

### 升级说明
- 本版与 v1.4.0 使用同一签名密钥，**可直接覆盖安装，微信连接与本地记忆都会保留、无需重新扫码**。
