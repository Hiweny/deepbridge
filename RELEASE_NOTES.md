## v1.6.0 更新内容

### 记忆压缩逻辑重写（滑窗 + 分批合并）
- **只保留一个数值「上下文窗口轮数 N」（默认 20，建议 20–30）**：它既是发给 AI 的可见上下文（最近 N 轮），也是一批记忆压缩的轮次，移除了此前容易混乱的「压缩阈值 / 会话轮换」两个独立轮数。
- 每攒满 N 轮新对话，就把这一整批折叠成摘要并与既有摘要**合并**；**原始历史完整保留、窗口不重置**，最近 N 轮始终原样发给 AI，保证对话连续。
- 当更早的批次完全滑出窗口后，再对新的 N 轮做总结并继续合并，从而**所有对话都会被覆盖到**，修复了此前「总是在压缩旧对话、新对话压不到」的问题。
- 每折叠一批自动把 DeepSeek 网站会话 New chat 以保持网站精简（本地记忆、计数、历史均保留）。

### 后台保活强化（vivo / OriginOS）
- **新增无障碍保活服务**：由系统绑定、进程优先级高、难被回收；检测到桥接服务被系统回收或轮询长时间卡死时自动重新拉起。在「更多设置 → 无障碍保活」一键跳转开启（不读取任何屏幕内容）。
- **常驻通知升级为实时状态**：下拉即可看到 AI 当前阶段（等待 / 处理中 / DeepSeek 思考生成中 / 已回复）、微信轮询是否在线、已收到与已回复条数及最近时间，并提供「重启桥接」快捷动作。
- 新增 **vivo/系统后台保活设置指引**：自启动、关联启动、后台高耗电（禁止限制）、卡片式后台下滑锁定，逐项引导。

### 说明
- 图片/文件传输、全屏沉浸、跟随系统深色、多条消息、思考过滤与撤回拦截等既有能力保持不变。
- 与历史版本使用**同一签名密钥，可直接覆盖安装，微信连接与本地记忆保留、无需重新扫码**。
