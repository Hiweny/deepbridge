## v1.4.0 更新内容

### 新增
- **微信图片/文件传输**：在微信直接发送图片或文件（PDF/Word/Excel/PPT/TXT/Markdown/代码/图片等），App 自动从微信 CDN 下载并 AES-128-ECB 解密，注入 DeepSeek 官网输入区，由官网完成上传与解析后连同文字一起发送；支持配文提问（如「总结这个文件」）。大图自动压缩，文档原样透传，单文件上限 25MB。
- 新增 `/文件开` `/文件关` 指令与控制台「文件传输」快捷开关。
- 自检诊断与状态中展示文件传输开关。

### 优化
- **记忆摘要篇幅自适应**：不再写死 300 字，按「既有摘要 + 待压缩对话」体量动态估算，下限 300 字、上限可在「更多设置」配置（默认最多 3000 字）。
- bridge.js 升级到 v3：同时 hook XHR 与 fetch，新增附件上传/解析完成度监测与多策略文件输入框注入，适配 DeepSeek 三模统一后的输入区。
- 开放完整源码，新增 GitHub Actions 自动构建签名 APK。

### 修复
- 重构并修正记忆压缩 Prompt 中「既有摘要/待压缩对话」的拼装逻辑。

### 安装说明
- 可直接覆盖安装历史版本；如提示签名不一致，请先卸载旧版再安装（本地记忆会保留在旧版数据内，卸载会清除）。
- 首次使用：「对话页」登录 DeepSeek → 控制台「连接微信」扫码 → 在微信 ClawBot 中直接聊天或发文件。
