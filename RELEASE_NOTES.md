## v1.5.1 更新内容

### 调整
- **移除「微信规则」Prompt 段**：不再向模型注入原生 emoji 清单与全屏彩蛋规则，界面更克制。
- **下线「Prompt 工程」多段编辑面板**：只保留唯一可编辑的**角色人格提示词**（在「更多设置 → 角色人设」修改，或微信发 `/人设 你的描述`，留空恢复默认）；多条消息规则、记忆总结模板回归内置固定，避免误改导致行为异常。
- **重写多条消息规则，显著提升 `\` 分隔符的正确输出率**：明确分隔符只能是半角反斜杠 `\`、给出正例与反例、说明常见错误写法（换行、`/`、顿号、全角＼、`\n`、`\\`），同时保留「普通回复不拆、最多 6 条」的克制约束。

### 说明
- 全屏沉浸、跟随系统深色、后台保活（心跳/看门狗/开机自启/划掉重启）、图片文件传输、自适应记忆摘要等 v1.5 能力保持不变。
- 与 v1.4/v1.5 使用同一签名密钥，**可直接覆盖安装，微信连接与本地记忆保留、无需重新扫码**。
